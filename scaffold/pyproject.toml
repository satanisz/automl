[project]
name = "automl-project"
version = "0.1.0"
description = "AutoML model project"
readme = "README.md"
requires-python = ">=3.9,<3.13"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["src"]
